<h1>Books</h1>
<% @books.each do |book| %>
  <div>
    <p>タイトル</p>
    <spen>
      <%=link_to book.title, book_path(book.id) %>
    </span>
    <p>感想<%= book.body %></p>
    <p>ユーザーネーム<%= book.user.name %></p>
  </div>
<% end %>